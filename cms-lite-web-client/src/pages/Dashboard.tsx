import {AppLayout} from '../layout';
import {SmartDashboard} from './SmartDashboard';

export const Dashboard = () => {
    return (
        <AppLayout>
            <SmartDashboard/>
        </AppLayout>
    );
}
